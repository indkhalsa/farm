<!DOCTYPE html>
<html>
    <head>
        <title>Assist</title>
    </head>

    <body>
        <h1>Add Good Detail</h1>

        <form id="GoodDetail_form" method="post" action="/assist/add_GoodDetail/">

            {% csrf_token %}
            {% for hidden in form.hidden_fields %}
                {{ hidden }}
            {% endfor %}

            {% for field in form.visible_fields %}
                {{ field.errors }}
                {{ field.help_text }}
                {{ field }}
            {% endfor %}

            <input type="submit" name="submit" value="Submit Good Detail" />
        </form>
    </body>

</html>
